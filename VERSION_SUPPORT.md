# 실행 파일 지원 정책 — R5

지원 대상은 Steam SP 빌드 24723416의 `iw6sp64_ship.exe`입니다.

SHA-256: `e9f62f2b780d2dce8180f4b5fd9f0022e10c9f2596cc8066128c21169b74f110`

파일 이름과 SHA-256 확인 뒤 실행 코드 검사 36개, 함수 검색 대상 31개의
단일 일치와 현재 주소 프로필 일치를 확인합니다. 모르는 EXE에서는
시스템 DXGI를 사용하고 게임 훅과 조명 변경을 적용하지 않습니다.

## 패턴 검색과 자동 업데이트의 차이

`GameAddressPatterns.inl` / `GameAddresses.cpp`가 함수 후보를 검색합니다.
누락·중복·프로필 불일치 시 함수 주소표를 공개하지 않으며, 검사하지 않은
고정 주소 호출로 되돌아가지 않습니다. 결과는 초기화 시 한 번 게시합니다.
읽을 수 없는 메모리 영역은 준비될 때까지 기다리지만, 실제 패턴 검사 실패
후 매 프레임 다시 검색하지 않습니다.

시험에서 이동한 함수 후보를 찾을 수 있더라도, 기존 호출 지점·전역 데이터·
구조체가 그대로 유효하다는 뜻은 아닙니다. 따라서 이동한 프로필은 런타임에서
거부합니다. 향후 EXE 업데이트 후 한글패치가 비활성화될 수 있습니다.
HUD 타이머·문구를 찾는 광범위 탐색은 별도 시스템이며 유지됩니다.

## 새 EXE 대응 절차

1. 패치 없이 정상 실행되는지 확인하고 EXE 이름·SHA-256·PE 정보를 기록합니다.
2. DLL 로딩 여부와 실제 실행 코드를 로컬에서 확인합니다.
3. 함수 패턴과 호출 관계, 인자·레지스터·구조체·전역 데이터 위치를 검토합니다.
4. 검증된 주소 프로필, 패턴, 파일 신원과 코드 검사를 함께 업데이트합니다.
5. 시작 메뉴·인트로·HUD·타이머·자막·조명·체크포인트를 실게임에서 확인합니다.

해시 제한만 해제해서 새 EXE를 허용하지 마세요. 패턴 발견만으로 함수의
호출 규약과 구조체 배치가 검증되는 것은 아닙니다.

Microsoft Store/Game Pass판과 멀티플레이는 이번 유지보수 범위에서 제외합니다.
