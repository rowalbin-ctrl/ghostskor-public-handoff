# 기존 스캐너가 있어도 8월 EXE 업데이트에서 깨진 이유

근거는 공개 소스 ZIP(a32baaee2b1b535f95c40c68bb8c61baa221bb78), 두 버전의 로컬 실행 코드 캡처, 기존 배포 DLL을 로드했을 때의 최초 충돌 기록이다. 아래 행 번호는 원본 공개 커밋의 소스 기준이다. 게임 실행 코드 캡처와 충돌 기록은 로컬 분석에만 사용했으며 이 저장소에 포함하지 않는다.

## 최초 충돌: 스캐너를 거치지 않은 고정 주소

- 원본 IW6Offsets.h:70의 `Dvar_FindVar_SP`는 `0x429E70`이었다.
- 원본 TextHook.SubtitleStringEd.inl:2554의 `DumpSubtitleDvarCandidates`는 모듈 기준 주소에 이 상수를 더해 직접 호출했다. 스캐너에 새 주소를 요청하지 않았다.
- 원본 TextHook.HookInstallAndDetours.inl:9404에서 초기 진단을 실행했다. 첫 조회 이름은 `cg_subtitleMinTime`이었다.
- 실제 충돌 기록의 RIP는 `0x140429E70`, RCX는 구 패치 DLL의 `cg_subtitleMinTime` 문자열, 반환 주소는 DLL RVA `0x6CA09`였다.
- 새 EXE의 이 주소는 다른 함수 중간이며 명령어 경계도 어긋났다. 이것이 관측한 최초 실행 충돌의 직접 원인이다.
- 로그 출력 비활성화만으로 진단용 게임 함수 호출이 제거되지는 않았다. 유지보수판은 해당 진단 코드를 별도 조건으로 제외하고, 필요한 주소를 새 EXE에 맞게 이식했다.

## 원본의 패턴 검색이 실제로 한 일

1. texthook.cpp:5732와 :6097은 목표 문구의 configstring 변환 함수와 인덱스 보정값을 찾는 대체 검색이다. EXE의 모든 함수와 데이터 주소를 이식하는 시스템은 아니다.
2. 이 두 경로가 쓰는 동일한 패턴을 로컬 실행 코드의 .text에서 검사했다. 2015판에서 7곳, 2026판에서도 7곳이 일치했다. 따라서 이 패턴 자체가 업데이트로 완전히 사라졌다는 설명은 맞지 않는다. 이는 두 버전의 로컬 캡처를 비교한 결과이며, 해당 캡처는 배포하지 않는다.
3. Utils.h:723의 FindPattern은 첫 번째 일치를 반환한다. 여러 일치 중 의도한 위치인지 확인하는 것은 호출 측의 책임이다. 이것은 설계상 한계이며 최초 충돌의 직접 원인으로 입증된 것은 아니다.
4. TextHook.HookInstallAndDetours.inl:8862의 함수 시작점 역탐색도 출발점 `0x1F087D`가 고정이다. 출발점 자체가 이동하면 다른 함수의 시작점을 찾을 수 있다.
5. 같은 파일 :10322의 텍스트 그리기 함수 선택은 고정된 기본/대체 주소의 시작 바이트를 검사한다. 임의의 새 주소를 검색하는 절차는 아니다.

## 판단

기존 패치에 실제 코드 패턴 검색은 있었다. 다만 검색·검증 대상 밖에 많은 고정 함수 주소, 호출 위치, 전역 데이터 주소가 남아 있었다. 일부 검색이 성공해도 그 나머지 주소가 자동으로 갱신되지는 않았다.

원본은 부분적인 패턴 검색과 고정 주소 호출이 혼합된 구현이었다.

현재 유지보수판은 알려진 Steam EXE의 파일/실행 코드 검증을 먼저 수행한다. 향후 자동 대응 범위를 넓히려면 사용 중인 모든 주소 의존성을 모으고, 함수별 검색·복수 후보 검증·호출 규약 및 구조체 검사·실패 시 해당 기능 중단까지 연결해야 한다. R4는 스캐너 범위를 유지했고, 후속 R5는 함수 31개의 공통 검색·검증과 주소 프로필 중앙화를 추가했다. 미래 EXE 자동 허용은 도입하지 않았다. 자세한 내용은 MAINTENANCE_R5.md를 참고한다.
